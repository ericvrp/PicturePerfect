{
  "name": "PicturePerfect",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "6.0.0"
  },
  "devDependencies": {
    "list": "^0.8.2",
    "now": "^0.28.1",
    "react-scripts": "^0.7.0"
  },
  "dependencies": {
    "googleapis": "^14.2.0",
    "react": "^15.4.0",
    "react-dom": "^15.4.0",
    "react-router": "^3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "start-server": "list build/",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "now -e REACT_APP_SECRET_CODE=secretcode"
  }
}
