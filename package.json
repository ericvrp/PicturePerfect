{
  "name": "PicturePerfect",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "googleapis": "^14.2.0",
    "list": "^0.8.2",
    "now": "^0.33.2",
    "react-scripts": "^0.7.0",
    "request": "^2.79.0",
    "sharp": "^0.16.2",
    "sw-precache": "^4.2.3"
  },
  "dependencies": {
    "md5": "^2.2.1",
    "react": "^15.4.1",
    "react-addons-shallow-compare": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-fastclick": "^2.1.2",
    "react-fullscreen": "^0.1.0",
    "react-router": "^3.0.0",
    "react-virtualized": "^8.7.1"
  },
  "scripts": {
    "start": "PORT=3002 react-scripts start",
    "start-prod": "npm run build && list -s --port 3002 build",
    "build": "rm -rf build && react-scripts build && sw-precache --config=sw-precache-config.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && now --static build"
  }
}
